apiVersion: v1
kind: List
metadata:
  name: analyze
items:
- apiVersion: v1
  kind: ImageStream
  metadata:
    name: analyze
    labels:
      app: analyze
- apiVersion: v1
  kind: BuildConfig
  metadata:
    name: analyze
    labels:
      app: analyze
  spec:
    triggers:
      -
        type: ImageChange
    source:
      type: Dockerfile
      dockerfile: |-
        FROM centos:7
        RUN yum install -y nc telnet traceroute scap-security-guide openscap-scanner openscap-utils
        RUN groupadd -r app -g 1000 && useradd -u 1000 -r -g app -m -d /app -s /sbin/nologin -c "Userapp" app && chmod 755 /app
        WORKDIR /app
        USER app
        CMD ["/bin/sh"]
    strategy:
      type: Docker
      dockerStrategy:
        from:
          kind: DockerImage
          name: "centos:7"
    output:
      to:
        kind: ImageStreamTag
        name: "analyze:latest"
